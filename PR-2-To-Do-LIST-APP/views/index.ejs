<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <title>To-Do Task Form</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: "Poppins", sans-serif;
        }

        body {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            /* <-- NEW: form upar, table neeche */
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
            padding: 20px;
            gap: 24px;
            /* form aur table ke beech thoda space */
        }

        .task-wrapper {
            background: #ffffff;
            max-width: 600px;
            width: 100%;
            border-radius: 16px;
            box-shadow: 0 18px 40px rgba(0, 0, 0, 0.18);
            padding: 24px 28px 30px;
            position: relative;
            overflow: hidden;
        }

        .task-wrapper::before {
            content: "";
            position: absolute;
            top: -60px;
            right: -60px;
            width: 160px;
            height: 160px;
            background: radial-gradient(circle at center, #ffb347, #ff416c);
            opacity: 0.18;
            z-index: 0;
        }

        .task-header {
            position: relative;
            z-index: 1;
            margin-bottom: 18px;
        }

        .task-badge {
            display: inline-block;
            padding: 4px 10px;
            font-size: 11px;
            text-transform: uppercase;
            letter-spacing: 1.4px;
            border-radius: 999px;
            background: rgba(37, 117, 252, 0.09);
            color: #2563eb;
            margin-bottom: 8px;
        }

        .task-header h2 {
            font-size: 22px;
            color: #111827;
            margin-bottom: 4px;
        }

        .task-header p {
            font-size: 13px;
            color: #6b7280;
        }

        form {
            position: relative;
            z-index: 1;
            margin-top: 6px;
        }

        .form-row {
            display: flex;
            gap: 14px;
            flex-wrap: wrap;
        }

        .form-group {
            margin-bottom: 14px;
            width: 100%;
        }

        .form-group.half {
            flex: 1 1 calc(50% - 7px);
        }

        label {
            display: inline-block;
            margin-bottom: 4px;
            font-size: 12px;
            font-weight: 500;
            color: #374151;
        }

        label span {
            color: #ef4444;
        }

        input[type="text"],
        input[type="datetime-local"],
        textarea,
        select {
            width: 100%;
            padding: 9px 11px;
            font-size: 13px;
            border-radius: 10px;
            border: 1px solid #e5e7eb;
            outline: none;
            transition: all 0.18s ease;
            background: #f9fafb;
        }

        textarea {
            min-height: 80px;
            resize: vertical;
        }

        input:focus,
        textarea:focus,
        select:focus {
            border-color: #2563eb;
            background: #ffffff;
            box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.12);
        }

        .status-pill {
            display: inline-flex;
            align-items: center;
            gap: 6px;
            font-size: 11px;
            padding: 3px 9px;
            border-radius: 999px;
            background: #fef3c7;
            color: #92400e;
            margin-left: 6px;
        }

        .status-dot {
            width: 7px;
            height: 7px;
            border-radius: 50%;
            background: #facc15;
        }

        .priority-high {
            color: #b91c1c;
            font-weight: 600;
        }

        .btn-row {
            display: flex;
            justify-content: flex-end;
            gap: 10px;
            margin-top: 4px;
        }

        .btn {
            border: none;
            outline: none;
            cursor: pointer;
            border-radius: 999px;
            padding: 9px 18px;
            font-size: 13px;
            font-weight: 500;
            display: inline-flex;
            align-items: center;
            gap: 6px;
            transition: transform 0.15s ease, box-shadow 0.15s ease, background 0.15s ease;
        }

        .btn-secondary {
            background: #e5e7eb;
            color: #374151;
        }

        .btn-secondary:hover {
            background: #d1d5db;
            transform: translateY(-1px);
        }

        .btn-primary {
            background: linear-gradient(135deg, #2563eb, #4f46e5);
            color: #ffffff;
            box-shadow: 0 10px 20px rgba(37, 99, 235, 0.35);
        }

        .btn-primary:hover {
            transform: translateY(-1px);
            box-shadow: 0 14px 26px rgba(37, 99, 235, 0.45);
        }

        .btn-primary:active,
        .btn-secondary:active {
            transform: translateY(0);
            box-shadow: none;
        }

        .btn-icon {
            font-size: 16px;
            line-height: 1;
        }

        .meta-info {
            display: flex;
            gap: 10px;
            flex-wrap: wrap;
            font-size: 11px;
            color: #9ca3af;
            margin-top: 4px;
        }

        .meta-info span {
            display: inline-flex;
            align-items: center;
            gap: 4px;
        }

        .dot {
            width: 4px;
            height: 4px;
            background: #d1d5db;
            border-radius: 50%;
        }

        @media (max-width: 480px) {
            .task-wrapper {
                padding: 20px 18px 22px;
                border-radius: 14px;
            }

            .task-header h2 {
                font-size: 18px;
            }
        }

        /* ========== TABLE STYLES (same as pehle) ========== */
        .task-list-wrapper {
            background: #ffffff;
            border-radius: 16px;
            padding: 18px 22px 22px;
            box-shadow: 0 16px 32px rgba(0, 0, 0, 0.22);
            width: 90%;
            max-width: 1100px;
            /* <-- table ab badi width me */
        }

        .task-list-header h3 {
            font-size: 18px;
            color: #111827;
            margin-bottom: 2px;
        }

        .task-list-header p {
            font-size: 12px;
            color: #6b7280;
            margin-bottom: 12px;
        }

        .task-table {
            width: 100%;
            border-collapse: collapse;
            font-size: 13px;
        }

        .task-table thead {
            background: #f3f4f6;
        }

        .task-table th,
        .task-table td {
            padding: 10px 12px;
            text-align: left;
        }

        .task-table th {
            font-weight: 600;
            font-size: 12px;
            text-transform: uppercase;
            letter-spacing: 0.06em;
            color: #4b5563;
            border-bottom: 1px solid #e5e7eb;
        }

        .task-table tbody tr {
            border-bottom: 1px solid #f1f5f9;
            transition: background 0.15s ease, transform 0.1s ease;
        }

        .task-table tbody tr:nth-child(even) {
            background: #f9fafb;
        }

        .task-table tbody tr:hover {
            background: #e5f0ff;
            transform: translateY(-1px);
        }

        .badge {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            padding: 2px 8px;
            border-radius: 999px;
            font-size: 11px;
            font-weight: 500;
        }

        .badge-status.pending {
            background: #fef3c7;
            color: #92400e;
        }

        .badge-status.in-progress {
            background: #dbeafe;
            color: #1d4ed8;
        }

        .badge-status.completed {
            background: #dcfce7;
            color: #166534;
        }

        .badge-priority.low {
            background: #e5e7eb;
            color: #374151;
        }

        .badge-priority.medium {
            background: #fee2e2;
            color: #b91c1c;
        }

        .badge-priority.high {
            background: #fed7d7;
            color: #b91c1c;
            font-weight: 600;
        }

        .actions-cell {
            display: flex;
            gap: 8px;
        }

        .table-btn {
            border: none;
            outline: none;
            cursor: pointer;
            font-size: 11px;
            padding: 6px 12px;
            border-radius: 999px;
            font-weight: 500;
            transition: background 0.15s ease, transform 0.1s ease, box-shadow 0.15s ease;
        }

        .edit-btn {
            background: #e0f2fe;
            color: #0369a1;
        }

        .edit-btn:hover {
            background: #bae6fd;
            transform: translateY(-1px);
            box-shadow: 0 6px 12px rgba(59, 130, 246, 0.3);
        }

        .delete-btn {
            background: #fee2e2;
            color: #b91c1c;
        }

        .delete-btn:hover {
            background: #fecaca;
            transform: translateY(-1px);
            box-shadow: 0 6px 12px rgba(248, 113, 113, 0.3);
        }

        @media (max-width: 600px) {
            .task-list-wrapper {
                padding: 14px 14px 18px;
                width: 100%;
            }

            .task-table th,
            .task-table td {
                padding: 8px 6px;
                font-size: 11px;
            }

            .actions-cell {
                flex-direction: column;
            }

            .table-btn {
                width: 100%;
                justify-content: center;
            }
        }
    </style>
</head>

<body>
    <!-- FORM CARD SAME -->
    <div class="task-wrapper">
        <div class="task-header">
            <div class="task-badge">To-Do Task</div>
            <h2>Create / Edit Task</h2>
            <p>Fill the details below to manage your task.</p>
        </div>

        <form action="/add-task" method="post">

            <div class="form-group">
                <label for="title">Id <span>*</span></label>
                <input type="text" id="id" name="id" placeholder="e.g. 1" required>
            </div>


            <div class="form-group">
                <label for="title">Title <span>*</span></label>
                <input type="text" id="title" name="title" placeholder="e.g. Complete Node.js Assignment" required>
            </div>

            <div class="form-group">
                <label for="description">Description <span>*</span></label>
                <textarea id="description" name="description"
                    placeholder="e.g. Finish CRUD operations in To-Do List project before Friday." required></textarea>
            </div>

            <div class="form-row">
                <div class="form-group half">
                    <label for="status">
                        Status
                        
                    </label>
                    <select id="status" name="status">
                        <option value="0">-- Select status -- </option>
                        <option value="pending">Pending</option>
                        <option value="in-progress">In Progress</option>
                        <option value="completed">Completed</option>
                    </select>
                </div>

                <div class="form-group half">
                    <label for="priority">Priority</label>
                    <select id="priority" name="priority">
                        <option value="0">-- Select Priority -- </option>
                        <option value="low">Low</option>
                        <option value="medium">Medium</option>
                        <option value="high">High</option>
                    </select>
                    
                </div>
            </div>

          

            <div class="form-group">
                <label for="dueDate">Due Date</label>
                <input type="datetime-local" id="dueDate" name="dueDate">
            </div>

            <div class="btn-row">
               
                <button type="submit" class="btn btn-primary">
                    <span class="btn-icon">✔</span> Save Task
                </button>
            </div>
        </form>
    </div>


    <div class="task-list-wrapper">
        <div class="task-list-header">
            <h3>Task List</h3>
            <!-- <p>All your tasks in one place. You can edit or delete them anytime.</p> -->
        </div>

        <table class="task-table">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Title</th>
                    <th>Description</th>
                    <th>Status</th>
                    <th>Priority</th>
                    <th>Due Date</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                <!-- <tr>
                    <td>1</td>
                    <td>Complete Node.js Assignment</td>
                    <td>
                        <span class="badge badge-status in-progress">In Progress</span>
                    </td>
                    <td>
                        <span class="badge badge-priority high">High</span>
                    </td>
                    <td>06 Dec 2025</td>
                    <td class="actions-cell">
                        <button class="table-btn edit-btn">Edit</button>
                        <button class="table-btn delete-btn">Delete</button>
                    </td>
                </tr>

                <tr>
                    <td>2</td>
                    <td>Study React Hooks</td>
                    <td>
                        <span class="badge badge-status pending">Pending</span>
                    </td>
                    <td>
                        <span class="badge badge-priority medium">Medium</span>
                    </td>
                    <td>08 Dec 2025</td>
                    <td class="actions-cell">
                        <button class="table-btn edit-btn">Edit</button>
                        <button class="table-btn delete-btn">Delete</button>
                    </td>
                </tr> -->

                <% data.forEach((data)=>{ %>
                    <tr>
                        <td>
                            <%= data.id %>
                        </td>
                        <td>
                            <%= data.title %>
                        </td>
                        <td>
                            <%= data.description %>
                        </td>
                        <td>
                            <%= data.status %>
                        </td>
                        <td>
                            <%= data.priority %>
                        </td>
                        <td>
                            <%= data.dueDate %>
                        </td>
                        <td class="actions-cell">
                            <a href="/edit-task/<%= data.id %>"> <button class="table-btn edit-btn">Edit</button></a>
                            <a href="/delete-task/<%= data.id %>"><button
                                    class="table-btn delete-btn">Delete</button></a>
                        </td>
                    </tr>
                    <% }) %>
            </tbody>
        </table>
    </div>

</body>

</html>